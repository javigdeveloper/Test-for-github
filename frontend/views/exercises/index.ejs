<!DOCTYPE html>
<html lang="en">
  <%- include('../partials/head.ejs') %>
  <body>
    <%- include('../partials/nav.ejs') %>
    <div class="content">
      <h1>Choose the subject you want to work on:</h1>
      <div class="links">
        <% if (result) { %>
          <% result.forEach( task => { %>
            <a href="/exercises/<%= task._id %>"><%= task.subject %></a>
          <% }) %>
        <% } %>
      </div>
    </div>
  </body>
</html>
